{
  "name": "study-buddy-one",
  "scripts": {
    "build": "cd sb_frontend && npm install && npm run build && cd ..",
    "postinstall": "npm run build && echo 'SB built!'"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "react-router-dom": "^5.0.1",
    "styled-components": "^4.3.1"
  }
}
